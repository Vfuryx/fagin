import { BasicFetchResult } from '/@/api/model/baseModel';

export type {{ camelCase name }}Params = {
  name?: string;
};

export interface {{ camelCase name }}ListItem {
  id: number;
}

export interface {{ camelCase name }}Item {
  id: number;
}

export type get{{ pascalCase name }}ListResultModel = BasicFetchResult<{{ camelCase name }}Item>;
