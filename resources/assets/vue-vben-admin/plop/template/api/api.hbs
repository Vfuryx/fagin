import { defHttp } from '/@/utils/http/axios';
import {
  get{{ pascalCase name }}ListResultModel,
  {{ camelCase name }}Item,
  {{ camelCase name }}Params,
} from '/@/api/{{ camelCase path }}/model/{{ camelCase name }}Model';

enum Api {
  {{ constantCase name }} = '{{ pascalCase name }}',
}

export const get{{ pascalCase name }}List = (params?: {{ camelCase name }}Params) => {
  return defHttp.get<get{{ pascalCase name }}ListResultModel>({ url: Api.{{constantCase name}}, params });
};

export const get{{ pascalCase name }}Detail = (id: number) => {
  return defHttp.get<{{ camelCase name }}Item>({ url: `${Api.{{constantCase name}} }/${id}` });
};

export const create{{ pascalCase name }} = (params: {{ camelCase name }}Params) => {
  return defHttp.post<{{ camelCase name }}Item>({ url: `${Api.{{constantCase name}} }`, params });
};

export const update{{ pascalCase name }} = (id: number, params: {{ camelCase name }}Params) => {
  return defHttp.put<{{ camelCase name }}Item>({ url: `${Api.{{constantCase name }} }/${id}`, params });
};

export const delete{{ pascalCase name }} = (id: number) => {
  return defHttp.delete({ url: `${Api.{{constantCase name }} }/${id}` });
};
