import { BasicColumn } from '/@/components/Table';
import { FormSchema } from '/@/components/Table';
import { h } from 'vue';
import { DescItem } from '/@/components/Description';
import { NbActive } from '/@/components/NbActive';

export const columns: BasicColumn[] = [
  {
    title: '编号',
    dataIndex: 'id',
    width: 160,
  },
  {
    title: '状态',
    dataIndex: 'active',
    width: 100,
    customRender: ({ record }) => h(NbActive, { active: ~~record.active !== 0 }),
  },
];

export const detailSchema: DescItem[] = [
  {
    field: 'id',
    label: '编号',
    labelMinWidth: 56,
  },
  {
    field: 'active',
    label: '状态',
    render: (_, record) => h(NbActive, { active: ~~record.active !== 0 }),
  },
];

export const searchFormSchema: FormSchema[] = [
  {
    field: 'name',
    label: '名称',
    component: 'Input',
    colProps: { span: 8 },
  },
  {
    field: 'active',
    label: '状态',
    component: 'NbActivePicker',
    colProps: { span: 8 },
  },
];

export const formSchema: FormSchema[] = [
  {
    field: 'name',
    label: '名称',
    component: 'Input',
    required: true,
  },
  {
    field: 'active',
    label: '状态',
    component: 'RadioButtonGroup',
    defaultValue: true,
    componentProps: {
      options: [
        { label: '启用', value: true },
        { label: '停用', value: false },
      ],
    },
    required: true,
  },
];
